access-key-id: ${AWS_ACCESS_KEY_ID}
secret-access-key: ${AWS_SECRET_ACCESS_KEY}
dbs:
  - path: ${DATABASE_PATH}
    replicas:
      - url: ${REPLICA_URL}
        sync-interval: 24h
        snapshot-interval: 72h
        retention: 72h
        retention-check-interval: 24h

